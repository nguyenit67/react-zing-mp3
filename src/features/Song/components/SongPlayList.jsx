// import SongMediaItem from './SongMediaItem';

// export default function SongPlayList({ songs = [] }) {
//   return (
//     <div className="zm-playlist">
//       {songs.map((song, index) => (
//         <SongMediaItem key={index} song={song} />
//       ))}
//     </div>
//   );
// }
